VITE_API_URL=http://localhost:5050/api

# Must match AUTH_MODE on the server.
#   cookie  session rides in an httpOnly cookie (default, and the safer one)
#   token   session is kept in localStorage and sent as `Authorization: Bearer`
VITE_AUTH_MODE=cookie
